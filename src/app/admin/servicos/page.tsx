"use client";
import {useEffect,useState}from'react';import Link from'react/link';
export default function Servicos(){const[data,setData]=useState<any[]>([]);const[f,setF]=useState({name:'',description:'',price:'',durationMin:'30'});useEffect(()=>{load()},[]);async function load(){const r=await fetch('/api/admin/services');const d=await r.json();setData(d)}async function create(e:any){e.preventDefault();await fetch('/api/admin/services',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});setF({name:'',description:'',price:'',durationMin:'30'});load()}async function update(a:any,patch:any){await fetch('/api/admin/services',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:a.id,...patch})});load()}async function toggle(a:any){update(a,{active:!a.active})}return <main className="dashboard"><div className="container"><Link href="/admin">← Dashboard</Link><div className="sectionhead"><div><div className="eyebrow">Gerenciamento</div><h1>Serviços</h1></div></div><form className="card" onSubmit={create}><div className="two"><div><div className="field"><label>Nome</label><input value={f.name} onChange={e=>setF({...f,name:e.target.value})} required/></div><div className="field"><label>Descrição</label><input value={f.description} onChange={e=>setF({...f,description:e.target.value})}/></div></div><div><div className="field"><label>Preço (R$)</label><input type="number" step="0.01" min="0" value={f.price} onChange={e=>setF({...f,price:e.target.value})} required/></div><div className="field"><label>Duração (min)</label><input type="number" min="1" value={f.durationMin} onChange={e=>setF({...f,durationMin:e.target.value})} required/></div></div></div><button className="btn">Criar serviço</button></form><div className="grid3" style={{marginTop:20}}>{data.map(a=><ServiceCard key={a.id} service={a} onSave={patch=>update(a,patch)} onToggle={()=>toggle(a)}/>)}</div></div></main>}
function ServiceCard({service,onSave,onToggle}:{service:any,onSave:(p:any)=>void,onToggle:()=>void}){const[name,setName]=useState(service.name),[description,setDescription]=useState(service.description),[price,setPrice]=useState((service.priceCents/100).toFixed(2)),[duration,setDuration]=useState(String(service.durationMin));return <div className="card"><div className="eyebrow">{service.active?'Ativo':'Desativado'}</div><div className="field"><label>Nome</label><input value={name} onChange={e=>setName(e.target.value)}/></div><div className="field"><label>Descrição</label><input value={description} onChange={e=>setDescription(e.target.value)}/></div><div className="field"><label>Preço (R$)</label><input type="number" step="0.01" value={price} onChange={e=>setPrice(e.target.value)}/></div><div className="field"><label>Duração (min)</label><input type="number" value={duration} onChange={e=>setDuration(e.target.value)}/></div><div className="actionsrow"><button className="btn" onClick={()=>onSave({name,description,price,durationMin:Number(duration)})}>Salvar</button><button className="btn alt" onClick={onToggle}>{service.active?'Desativar':'Ativar'}</button></div></div>}
