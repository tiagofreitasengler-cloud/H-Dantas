"use client";
import {useEffect,useState}from'react';import Link from'next/link';import LogoutButton from'@/components/LogoutButton';
export default function Admin(){const[s,setS]=useState<any>({});useEffect(()=>{fetch('/api/admin/stats').then(r=>r.json()).then(setS)},[]);return <main className="dashboard"><div className="container dashgrid"><aside className="sidebar"><Link className="sideitem active" href="/admin">Dashboard</Link><Link className="sideitem" href="/admin/agenda">Agenda</Link><Link className="sideitem" href="/admin/horarios">Horários</Link><Link className="sideitem" href="/admin/servicos">Serviços</Link><LogoutButton/></aside><section><div className="eyebrow">Administração</div><h1>Visão geral</h1><div className="statgrid"><div className="card stat"><div className="muted">Agendamentos hoje</div><h3>{s.todayCount??'—'}</h3></div><div className="card stat"><div className="muted">Próximos</div><h3>{s.nextCount??'—'}</h3></div><div className="card stat"><div className="muted">Clientes</div><h3>{s.clients??'—'}</h3></div><div className="card stat"><div className="muted">Concluídos</div><h3>{s.done??'—'}</h3></div></div><div className="card" style={{marginTop:20}}><h2>Pedro Henrique Dantas</h2><p className="muted">Administrador e único barbeiro da H'Dantas.</p></div></section></div></main>}
