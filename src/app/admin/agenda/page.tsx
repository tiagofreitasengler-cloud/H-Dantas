"use client";
import {useEffect,useState}from'react';import Link from'next/link';
export default function Agenda(){const[data,setData]=useState<any[]>([]);const[filter,setFilter]=useState('');useEffect(()=>{fetch('/api/admin/appointments').then(r=>r.json()).then(d=>setData(Array.isArray(d)?d:[]))},[]);async function status(id:number,status:string){await fetch('/api/admin/appointments',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,status})});setData(x=>x.map(a=>a.id===id?{...a,status}:a))}const shown=data.filter(a=>!filter||a.date===filter);return <main className="dashboard"><div className="container"><Link href="/admin">← Dashboard</Link><div className="sectionhead"><div><div className="eyebrow">Agenda administrativa</div><h1>Hoje · Semana · Mês</h1></div><input type="date" value={filter} onChange={e=>setFilter(e.target.value)}/></div><div className="tablewrap"><table className="table"><thead><tr><th>Data</th><th>Horário</th><th>Cliente</th><th>Serviço</th><th>Telefone</th><th>Status</th><th>Ações</th></tr></thead><tbody>{shown.map(a=><tr key={a.id}><td>{a.date.split('-').reverse().join('/')}</td><td>{a.time}</td><td>{a.user.name}</td><td>{a.service.name}</td><td>{a.user.phone}</td><td><span className="badge">{a.status}</span></td><td><div className="actionsrow">{a.status==='AGENDADO'&&<button className="btn" onClick={()=>status(a.id,'CONFIRMADO')}>Confirmar</button>}{a.status!=='CONCLUIDO'&&a.status!=='CANCELADO'&&<button className="btn alt" onClick={()=>status(a.id,'CONCLUIDO')}>Concluir</button>}{a.status!=='CANCELADO'&&a.status!=='CONCLUIDO'&&<button className="btn alt danger" onClick={()=>status(a.id,'CANCELADO')}>Cancelar</button>}</div></td></tr>)}</tbody></table></div></div></main>}
