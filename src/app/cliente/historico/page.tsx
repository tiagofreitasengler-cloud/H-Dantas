import {requireUser}from'@/lib/auth';import {prisma}from'@/lib/prisma';import {money}from'@/lib/format';
export default async function Historico(){const u=await requireUser();const data=await prisma.appointment.findMany({where:{userId:u.id,status:{in:['CANCELADO','CONCLUIDO']}},include:{service:true},orderBy:[{date:'desc'},{time:'desc'}]});return <main className="dashboard"><div className="container"><a href="/cliente">← Voltar</a><h1>Histórico</h1><div className="grid3">{data.map(a=><article className="card" key={a.id}><div className="eyebrow">{a.status}</div><h2>{a.service.name}</h2><p className="muted">{a.date.split('-').reverse().join('/')} às {a.time}<br/>{money(a.valueCents)}</p></article>)}{data.length===0&&<p className="muted">Você ainda não possui histórico de atendimentos.</p>}</div></div></main>}
