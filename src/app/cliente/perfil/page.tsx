"use client";
import {useEffect,useState}from'react';import Link from'react/link';
export default function Perfil(){const[f,setF]=useState({name:'',email:'',phone:'',newPassword:''});const[msg,setMsg]=useState('');const[err,setErr]=useState('');useEffect(()=>{fetch('/api/auth/me').then(r=>r.json()).then(d=>d?.email&&setF({name:d.name,email:d.email,phone:d.phone,newPassword:''}))},[]);async function save(e:any){e.preventDefault();setMsg('');setErr('');const r=await fetch('/api/auth/me',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});const d=await r.json();if(!r.ok){setErr(d.error||'Erro');return}setMsg('Perfil atualizado com sucesso.');setF(x=>({...x,newPassword:''}))}return <main className="dashboard"><div className="container"><Link href="/cliente">← Voltar</Link><h1>Meu perfil</h1><form className="formcard" onSubmit={save}><div className="field"><label>Nome</label><input value={f.name} onChange={e=>setF({...f,name:e.target.value})} required/></div><div className="field"><label>E-mail</label><input value={f.email} disabled/></div><div className="field"><label>Telefone</label><input value={f.phone} onChange={e=>setF({...f,phone:e.target.value})} required/></div><div className="field"><label>Nova senha</label><input type="password" value={f.newPassword} onChange={e=>setF({...f,newPassword:e.target.value})} minLength={8}/></div>{err&&<div className="error">{err}</div>}{msg&&<div className="success">{msg}</div>}<button className="btn">Salvar alterações</button></form></div></main>}
