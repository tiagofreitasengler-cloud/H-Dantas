"use client";
import {FormEvent,useState} from 'react';
export default function Cadastro(){const [f,setF]=useState({name:'',email:'',phone:'',password:'',confirmPassword:''});const[error,setError]=useState('');const[loading,setLoading]=useState(false);const set=(k:string,v:string)=>setF(x=>({...x,[k]:v}));async function submit(e:FormEvent){e.preventDefault();setError('');setLoading(true);const r=await fetch('/api/auth/register',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(f)});const d=await r.json();setLoading(false);if(!r.ok){setError(d.error||'Não foi possível cadastrar');return}location.href='/cliente'}return <main className="authwrap"><div className="container"><form className="formcard" onSubmit={submit}><div className="eyebrow">Cliente</div><h1>Criar minha conta</h1>{error&&<div className="error">{error}</div>}{[['name','Nome completo','text'],['email','E-mail','email'],['phone','Telefone','tel'],['password','Senha','password'],['confirmPassword','Confirmar senha','password']].map(([k,l,t])=><div className="field" key={k}><label>{l}</label><input type={t} value={(f as any)[k]} onChange={e=>set(k,e.target.value)} required /></div>)}<button className="btn" disabled={loading}>{loading?'Criando...':'Criar minha conta'}</button></form></div></main>}
