"use client";
import {useState}from'react';import {useRouter}from'next/navigation';
export default function AdminLogin(){const[e,setE]=useState(''),[p,setP]=useState(''),[err,setErr]=useState('');const r=useRouter();async function go(ev:any){ev.preventDefault();setErr('');const x=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:e,password:p})});const d=await x.json();if(!x.ok||d.role!=='ADMIN'){setErr('Credenciais administrativas inválidas.');return}r.push('/admin')}return <main className="authwrap"><div className="container"><form className="formcard" onSubmit={go}><div className="eyebrow">H'Dantas · Administração</div><h1>Entrar no painel</h1>{err&&<div className="error">{err}</div>}<div className="field"><label>E-mail</label><input type="email" value={e} onChange={x=>setE(x.target.value)} required/></div><div className="field"><label>Senha</label><input type="password" value={p} onChange={x=>setP(x.target.value)} required/></div><button className="btn">Entrar</button></form></div></main>}
