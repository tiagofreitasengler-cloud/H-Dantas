"use client";
import {FormEvent,useState} from 'react';import Link from 'next/link';
export default function Login(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError('');setLoading(true);const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();setLoading(false);if(!r.ok){setError(d.error||'Erro ao entrar');return}location.href=d.role==='ADMIN'?'/admin':'/cliente'}return <main className="authwrap"><div className="container"><form className="formcard" onSubmit={submit}><div className="eyebrow">Acesso</div><h1>Entrar</h1><p className="muted">Acesse sua conta H'Dantas.</p>{error&&<div className="error">{error}</div>}<div className="field"><label>E-mail</label><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></div><div className="field"><label>Senha</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></div><button className="btn" disabled={loading}>{loading?'Entrando...':'Entrar'}</button><p className="small">Ainda não tem conta? <Link className="gold" href="/cadastro">Criar conta</Link></p></form></div></main>}
